pipeline {

  agent any
  
  stagess {
  
    stage("build") {
      steps {
        echo "build the app.."
      }
    }
    
    stage("test") {
      steps {
        echo "test the app..."
      }
    }
    
    stage("deploy") {
      steps {
        echo "deploy the app..."
      }
    }
  
  }
  
}
